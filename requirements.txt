# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.12      # requerido por FastAPI para recibir form-data y archivos

# --- Base de datos ---
supabase==2.10.0

# --- Google Drive ---
google-api-python-client==2.154.0
google-auth==2.36.0
google-auth-httplib2==0.2.0   # requerido por google-api-python-client para el transporte HTTP
google-auth-oauthlib==1.2.1   # flujo OAuth 2.0 para credenciales de usuario

# --- Clasificación automática (subida masiva) ---
pymupdf==1.28.0               # rasteriza la 1a página de PDFs a PNG para el modelo de visión

# --- Utilidades ---
python-dotenv==1.0.1
